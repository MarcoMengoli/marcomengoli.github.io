---
interface Props {
  id?: string;
}

const { id } = Astro.props;
---

<h2
  id={id}
  class="text-3xl font-extrabold tracking-tight text-slate-900 dark:text-white sm:text-4xl"
>
  <slot />
  <div class="mt-3 h-1 w-12 rounded-full bg-accent-500"></div>
</h2>
